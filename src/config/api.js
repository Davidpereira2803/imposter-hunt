import { API_BASE_URL, API_KEY } from "@env";

const API_CONFIG = {
  BASE_URL: API_BASE_URL || "https://imposter-hunt-prokhrske-pearlabs-projects.vercel.app",
  API_KEY: API_KEY || "",
};

export default API_CONFIG;